extratos/*.xlsx
*.xlsx
*.mhtml
*.pdf
scratch_artifact.html
painel/
__pycache__/
*.pyc
webapp_data/
# Dicionário departamento -> código opaco de URL (mora dentro de webapp_data/,
# então já não vai pro git; explícito aqui só pra deixar claro que é secreto).
webapp_data/deptos_codigos.json
painel_web/

# Worktree local do deploy_github.py
.gh-pages-worktree/

.env

# Ambiente virtual Python
.venv/
